# Configuração Gemini (Insights — Jaraguá)
#
# NÃO commite chaves reais no Git.
#
# Render (obrigatório para Insights funcionar)
# 1. Abra o serviço **Web** (Flask / gunicorn) — o mesmo que responde /analytics.
#    NÃO use só o Static Site: a chave tem de estar no processo Python.
# 2. Environment → Add Environment Variable:
#      Name:  GEMINI_API_KEY
#      Value: (cole a chave do Google AI Studio ou Google Cloud)
# 3. Save Changes → Manual Deploy (ou aguarde o deploy automático).
#
# Opcional:
#   GEMINI_MODEL = gemini-2.0-flash
#
# Se uma chave foi exposta, revogue no Google e crie outra.
#
# Código: backend/legacy/app.py → POST /insights
